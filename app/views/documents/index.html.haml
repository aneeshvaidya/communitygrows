// #docRepo
//   %h1 Document Repository
  
// = link_to 'Add new file', new_file_path, :class => "button"

// - if flash[:notice]
//   #flashNotice
//     = flash[:notice]

// %h2#committeeTitle Executive committee 

// %table#docs
//   %thead#filebar
//     %tr
//       %td File name
//       %td URL
//       %td Updated at
//       %td Edit
//       %td Delete
//   %tbody
//     - documents = Document.where(:committee_type => 'executive')
//     - if documents.empty?
//       %td= "No files have been entered"
//     - documents.each do |file|
//       %tr
//         %td= file.title
//         %td= link_to file.url, url_for(file.url)
//         %td= file.updated_at
//         %td= link_to "Edit document", edit_file_path(file.id)
//         %td= link_to "Delete document", delete_file_path(file.id), data: { confirm: 'Are you sure?' }
        
// %h2#committeeTitle External committee

// %table#docs
//   %thead#filebar
//     %tr
//       %td File name
//       %td URL
//       %td Updated at
//       %td Edit
//       %td Delete
//   %tbody
//     - documents = Document.where(:committee_type => 'external')
//     - if documents.empty?
//       %td= "No files have been entered"
//     - documents.each do |file|
//       %tr
//         %td= file.title
//         %td= file.url
//         %td= file.updated_at
//         %td= link_to "Edit document", edit_file_path(file.id)
//         %td= link_to "Delete document", delete_file_path(file.id), data: { confirm: 'Are you sure?' }
        
// %h2#committeeTitle Internal committee

// %table#docs
//   %thead#filebar
//     %tr
//       %td File name
//       %td URL
//       %td Updated at
//       %td Edit
//       %td Delete
//   %tbody
//     - documents = Document.where(:committee_type => 'internal')
//     - if documents.empty?
//       %td= "No files have been entered"
//     - documents.each do |file|
//       %tr
//         %td= file.title
//         %td= file.url
//         %td= file.updated_at
//         %td= link_to "Edit document", edit_file_path(file.id)
//         %td= link_to "Delete document", delete_file_path(file.id), data: { confirm: 'Are you sure?' }

#docRepo
  %h1 Document Repository
  
= link_to 'Add new file', new_file_path, :class => "button"

- if flash[:notice] and flash[:notice]!="Signed in successfully."
  #flashNotice
    = flash[:notice]

%h2#committeeTitle CommunityGrows Administration

%table#docs
  %thead#filebar
    %tr
      %td File
      %td Updated at
      %td Edit
      %td Delete
  %tbody
    // - documents = Document.where(:committee_type => 'executive')
    - documents = Document.where(:committee_type => 'CGadmin')
    - if documents.empty?
      %td= "No files have been entered"
    - documents.each do |file|
      %tr
        %td= link_to file.title, url_for(file.url)
        %td= file.updated_at
        %td= link_to "Edit document", edit_file_path(file.id)
        %td= link_to "Delete document", delete_file_path(file.id), data: { confirm: 'Are you sure?' }
        
%h2#committeeTitle Board Overview

%table#docs
  %thead#filebar
    %tr
      %td File
      %td Updated at
      %td Edit
      %td Delete
  %tbody
    // - documents = Document.where(:committee_type => 'external')
    - documents = Document.where(:committee_type => 'boardoverview')
    
    - if documents.empty?
      %td= "No files have been entered"
    - documents.each do |file|
      %tr
        %td= link_to file.title, url_for(file.url)
        %td= file.updated_at
        %td= link_to "Edit document", edit_file_path(file.id)
        %td= link_to "Delete document", delete_file_path(file.id), data: { confirm: 'Are you sure?' }
        
%h2#committeeTitle Board Activities

%table#docs
  %thead#filebar
    %tr
      %td File
      %td Updated at
      %td Edit
      %td Delete
  %tbody
    // - documents = Document.where(:committee_type => 'internal')
    - documents = Document.where(:committee_type => 'boardactivities')
    
    - if documents.empty?
      %td= "No files have been entered"
    - documents.each do |file|
      %tr
        %td= link_to file.title, url_for(file.url)
        %td= file.updated_at
        %td= link_to "Edit document", edit_file_path(file.id)
        %td= link_to "Delete document", delete_file_path(file.id), data: { confirm: 'Are you sure?' }

%h2#committeeTitle Budgets and Finances

%table#docs
  %thead#filebar
    %tr
      %td File name
      %td URL
      %td Updated at
      %td Edit
      %td Delete
  %tbody
    - documents = Document.where(:committee_type => 'budgetfinance')
    - if documents.empty?
      %td= "No files have been entered"
    - documents.each do |file|
      %tr
        %td= file.title
        %td= file.url
        %td= file.updated_at
        %td= link_to "Edit document", edit_file_path(file.id)
        %td= link_to "Delete document", delete_file_path(file.id), data: { confirm: 'Are you sure?' }


%h2#committeeTitle AB Meetings
%table#docs
  %thead#filebar
    %tr
      %td File name
      %td URL
      %td Updated at
      %td Edit
      %td Delete
  %tbody
    - documents = Document.where(:committee_type => 'abmeetings')
    
    - if documents.empty?
      %td= "No files have been entered"
    - documents.each do |file|
      %tr
        %td= file.title
        %td= file.url
        %td= file.updated_at
        %td= link_to "Edit document", edit_file_path(file.id)
        %td= link_to "Delete document", delete_file_path(file.id), data: { confirm: 'Are you sure?' }
